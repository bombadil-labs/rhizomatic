# Conformance Vectors

Language-agnostic test data. **This directory is the source of truth for correctness.** Every
conformant implementation — TypeScript, Rust, and any future citizen — is tested against these exact
files and must produce byte-identical canonical output.

Vectors are plain JSON so any language can load them. Binary values (canonical CBOR, hashes,
signatures, keys) are encoded as **lowercase hex strings**. Where a field is "canonical," the bytes
are normative: implementations compare bytes, not parsed structures.

## Layout

```
vectors/
  l0-delta/
    cbor-primitives.json   hand-verified scalar ground truth (anchors both encoders) — incl. bstr (0.4)
    deltas.json            claims -> (canonical CBOR hex, id); generated by TS, reproduced by Rust
    deltas-signed.json     signed deltas: + keyId and deterministic Ed25519 sig bytes (ERRATA D8-D9)
    deltas-bytes.json      the bytes target kind: (mime, value) -> canonical CBOR hex + id (0.4, D12)
    deltas-invalid.json    malformed claims that MUST be rejected at the boundary (SPEC-4 §2)
    set-digest.json        canonical membership digest of the deltas.json set (ERRATA D10, provisional)
  l0-pack/
    pack.json              L0 pack round-trip: delta set -> (pack bytes, packId) (SPEC-8, M3)
    pack-bytes.json        pack round-trip over bytes targets: m/y pointer records (0.4, SPEC-8)
  keys/
    keys.json              test keypairs: keyId, seedHex, publicKeyHex, author (deterministic seeds)
  l1-eval/                 SPEC-2 / SPEC-3 / SPEC-5: operator-algebra evaluation
    eval-bytes.json        resolve over bytes candidates: View leaf, conflicts, merge-transparency (0.4)
```

(Levels here name the conformance level being exercised, per SPEC-0 §5.1 — Level 0 = Format,
Level 1 = Evaluator.)

## Vector shapes

### Delta / canonicalization (`l0-delta/`)

```json
{
  "name": "signed-single-claim",
  "spec": "SPEC-1 §5 / ERRATA D8-D9",
  "keyId": "test-key-1",
  "claims": { "timestamp": 0, "author": "ed25519:<pubkey hex>", "pointers": [ ... ] },
  "canonicalCborHex": "a3...",
  "id": "1e20...",
  "sig": "<128 hex chars>"
}
```

- `claims` is the human-readable input (the JSON debug profile — see the ERRATA; numbers MUST be
  parsed correctly rounded).
- `canonicalCborHex` is the normative deterministic-CBOR encoding of `claims` (RFC 8949 §4.2.1).
- `id` is the BLAKE3-256 multihash of those bytes, lowercase hex.
- `keyId`/`sig` (signed vectors only): detached deterministic Ed25519 signature over the raw id
  bytes by the named test key in `keys/keys.json`. Implementations must *reproduce* the signature
  bytes, not merely verify them.

### Bytes target (`l0-delta/deltas-bytes.json`, 0.4)

Same envelope as the delta shape above (`name`, `spec`, `claims`, `canonicalCborHex`, `id`; a
`keyId`+`sig` on the one signed case). The distinguishing part is the pointer target, a bytes
literal in the JSON debug profile:

```json
{ "role": "icon", "target": { "mime": "image/png", "value": "iVBORw0KGgo" } }
```

- `value` is **canonical base64url** (RFC 4648 §5, unpadded — SPEC-1 §4.2); the empty payload is
  `""`.
- `canonicalCborHex` encodes the target as `map { "mime": tstr, "value": bstr }` (SPEC-1 §4.1); the
  raw payload bytes are the `bstr`, so the empty payload contributes `0x40`.
- Coverage: zero-length payload; the same bytes under `image/png` vs `image/PNG` producing
  **different** ids (mime is case-sensitive); `application/wasm` on the same bytes (different claim);
  a mixed-pointer delta (bytes + filing EntityRef + string primitive); a ≥24-byte payload (the
  `0x58 <len>` head path); 1- and 2-byte payloads (the nontrivial b64u tail shapes); one signed case.
- Rejections for the kind live in `deltas-invalid.json` (appended): missing/empty/non-NFC `mime`,
  non-string `mime`/`value`, and the base64url failures (padded, bad alphabet, bad length,
  non-canonical trailing bits).

`l0-pack/pack-bytes.json` pins the pack round-trip (two bytes deltas sharing one mime → the `m`
intern + raw `y`, SPEC-8). `l1-eval/eval-bytes.json` pins resolution: `pick` yielding a bytes View
leaf (both `expectedView` JSON and the leaf's `expectedCanonicalHex`), `conflicts` over
identical-vs-differing `(mime, bytes)`, and `merge` folding past bytes while `count` counts them.

### Invalid deltas (`l0-delta/deltas-invalid.json`)

```json
{
  "name": "primitive-null",
  "spec": "SPEC-1 §2.1 / SPEC-4 §2",
  "reason": "null is not a primitive; must be string | number | boolean",
  "claims": { /* malformed on purpose */ }
}
```

Each entry MUST be rejected before canonical bytes exist — no `canonicalCborHex`, no `id`.
*Where* an implementation rejects (profile parser vs. claims validation) is implementation
detail; error messages are not pinned. Reject, never repair: no coercion, no dropping of
malformed fields (a repaired delta would canonicalize to different bytes in different
implementations, which is how silent divergence starts).

### Evaluation (`l1-eval/`)

```json
{
  "name": "select-then-group-with-negation",
  "spec": "SPEC-2 §4 / SPEC-3 §2",
  "deltas": [ /* input delta set, claims form */ ],
  "schemaProgram": { /* an L2 term, JSON-encoded */ },
  "policy": { /* optional: an L5 policy term, for resolve vectors */ },
  "rootEntity": "...",
  "expectedCanonicalHex": "..."
}
```

`expectedCanonicalHex` is the canonical serialization of the result (HyperView or View). Two
implementations are at parity when both reproduce it byte-for-byte.

## Rules

- A vector is added **before or with** the code it pins (see [CLAUDE.md](../CLAUDE.md)).
- If a vector is wrong, fix the vector (and the spec, if it was faithfully wrong) — never patch one
  implementation to match a bad vector.
- Cover edge cases explicitly: negation chains (even/odd length), pointer permutations, empty and
  all-negated properties, mixed primitive types, divergent transaction members.
